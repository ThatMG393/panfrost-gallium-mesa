diff a/src/gallium/targets/rusticl/meson.build b/src/gallium/targets/rusticl/meson.build	(rejected hunks)
@@ -42,13 +49,23 @@ librusticl = shared_library(
     libws_null,
   ],
   dependencies : [
+    driver_asahi,
     driver_iris,
     driver_nouveau,
+    driver_r600,
     driver_panfrost,
+    driver_radeonsi,
     driver_swrast,
+    driver_zink,
     idep_nir,
+    idep_vtn,
   ],
-  link_args: ld_args_build_id,
+  link_args : [
+    ld_args_build_id,
+    ld_args_gc_sections,
+    rusticl_ld_args,
+  ],
+  link_depends : rusticl_ld_depends,
   version : '@0@.0.0'.format(opencl_version),
   install : true,
 )
